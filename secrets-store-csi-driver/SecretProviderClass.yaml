apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: nginx-secretproviderclass
spec:
  provider: aws
  parameters:
    objects: |
      - objectName: "csiDriverEKS"
        objectType: "secretsmanager"
    usePodIdentity: "true"
